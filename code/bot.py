import ConfigParser as cp #we will need it to read the config file which we will create later will contain voice and id
import os

DirName='/'.join(os.path.dirname(os.path.realpath(__file__)).split('/')[:-1])